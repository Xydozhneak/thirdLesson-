firstValue=prompt('Enter 1st number' );
lastValue=prompt('Enter 2nd number' );

difference=firstValue-lastValue;
summ= parseFloat(firstValue)+parseFloat(lastValue);
multipl=firstValue*lastValue;
divide=firstValue/lastValue;
alert(` ${firstValue} + ${lastValue} = ${summ} 

${firstValue}-${lastValue}= ${difference}

${firstValue}*${lastValue}=${multipl}

${firstValue}/${lastValue}=${divide}

`);